{
  "name": "castle",
  "version": "0.1.0",
  "description": "A local-first desktop and web application for Markdown knowledge bases.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "sync:person-locations": "cargo xtask sync-person-locations",
    "check:person-locations": "cargo xtask sync-person-locations --check",
    "generate:icons": "cargo xtask generate icons",
    "generate:contracts": "cargo xtask generate contracts",
    "build:native": "cargo xtask build native",
    "test:native": "cargo xtask test native",
    "generate:content": "cargo xtask generate content desktop",
    "generate:content:web": "cargo xtask generate content web",
    "generate:content:web:technical": "cargo xtask generate content web-technical",
    "generate": "cargo xtask generate",
    "validate:library": "cargo xtask validate-library",
    "migrate:records": "cargo xtask migrate-records",
    "dev": "cargo xtask dev",
    "dev:viewer": "cargo xtask dev viewer",
    "dev:web": "cargo xtask dev web",
    "dev:desktop": "cargo xtask dev desktop",
    "typecheck": "cargo xtask typecheck",
    "check:architecture": "cargo xtask check-architecture",
    "format:check": "cargo xtask format --check",
    "lint:web": "cargo xtask lint web",
    "lint:native": "cargo xtask lint native",
    "lint": "cargo xtask lint",
    "build": "cargo xtask build",
    "build:viewer": "cargo xtask build viewer",
    "build:web": "cargo xtask build web",
    "build:web:technical": "cargo xtask build web-technical",
    "build:cloudflare": "node apps/web/scripts/build-cloudflare.mjs",
    "test": "cargo xtask test javascript",
    "check": "cargo xtask check",
    "preview": "cargo xtask preview",
    "preview:viewer": "cargo xtask preview viewer",
    "preview:web": "cargo xtask preview web",
    "package:desktop": "cargo xtask package-desktop",
    "make:desktop": "cargo xtask make-desktop",
    "deploy": "cargo xtask deploy",
    "deploy:web": "cargo xtask deploy"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.15.0",
    "eslint": "^10.8.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.9.0",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.65.0",
    "wrangler": "4.127.1"
  }
}
