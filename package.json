{
  "name": "castle",
  "productName": "Castle",
  "version": "0.1.0",
  "description": "A local-first desktop and web application for Markdown knowledge bases.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "main": ".vite/build/main.cjs",
  "scripts": {
    "sync:person-locations": "node scripts/sync-person-locations.mjs",
    "check:person-locations": "node scripts/sync-person-locations.mjs --check",
    "generate:icons": "node scripts/generate-blueprint-icon-loader.mjs",
    "generate:contracts": "cargo run --quiet --manifest-path native/Cargo.toml -p castle-contracts --bin export-contracts -- packages/contracts/src/castle_contract_schema.json && node scripts/generate-contracts.mjs",
    "build:native": "cargo build --release --quiet --manifest-path native/Cargo.toml -p castle-cli",
    "test:native": "cargo test --manifest-path native/Cargo.toml --workspace",
    "generate:content": "cargo run --release --quiet --manifest-path native/Cargo.toml -p castle-web-build -- build",
    "generate:content:web": "cargo run --release --quiet --manifest-path native/Cargo.toml -p castle-web-build -- build --public apps/web/.castle/public",
    "generate": "npm run sync:person-locations && npm run generate:contracts && npm run generate:content && npm run generate:icons",
    "validate:library": "npm run check:person-locations && cargo run --release --quiet --manifest-path native/Cargo.toml -p castle-cli -- validate",
    "migrate:records": "cargo run --quiet --manifest-path native/Cargo.toml -p castle-cli -- migrate",
    "dev": "npm run dev:web",
    "dev:viewer": "npm run generate && vite --host 127.0.0.1",
    "dev:web": "npm run generate:content:web && npm run dev --workspace @castle/web",
    "dev:desktop": "npm run generate && electron-forge start",
    "typecheck": "tsc --noEmit && tsc -p config/typescript/scripts.json && tsc -p config/typescript/electron.json",
    "check:architecture": "node scripts/check-architecture.mjs",
    "format:check": "cargo fmt --manifest-path native/Cargo.toml --all --check",
    "lint:web": "npm run check:architecture && eslint --max-warnings=0 src electron packages scripts test config vite.config.ts",
    "lint:native": "cargo clippy --manifest-path native/Cargo.toml --workspace --all-targets -- -D warnings",
    "lint": "npm run lint:web && npm run lint:native",
    "build": "npm run build:web",
    "build:viewer": "npm run check:person-locations && npm run generate:contracts && npm run generate:content && npm run generate:icons && tsc --noEmit && vite build",
    "build:web": "npm run check:person-locations && npm run generate:content:web && npm run build --workspace @castle/web",
    "build:cloudflare": "node scripts/build-cloudflare.mjs",
    "test": "node --preserve-symlinks --import tsx --test",
    "check": "npm run format:check && npm run lint && npm run test:native && npm run test && npm run build:viewer && npm run build:web && npm run typecheck",
    "preview": "npm run preview:web",
    "preview:viewer": "vite preview --host 127.0.0.1",
    "preview:web": "npm run preview --workspace @castle/web",
    "package:desktop": "npm run generate && electron-forge package",
    "make:desktop": "npm run generate && electron-forge make",
    "deploy": "node ./scripts/check-private-deploy.mjs && npm run check && wrangler deploy",
    "deploy:web": "npm run deploy"
  },
  "dependencies": {
    "@blueprintjs/icons": "^6.2.0",
    "@castle/content": "0.1.0",
    "@castle/contracts": "0.1.0",
    "@patkepa/kantzen-ui": "0.1.0",
    "@videojs/react": "10.0.0-beta.28",
    "github-slugger": "^2.0.0",
    "gray-matter": "^4.0.3",
    "leaflet": "^1.9.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-leaflet": "^4.2.1",
    "react-markdown": "^10.1.0",
    "react-router-dom": "^7.18.2",
    "remark-breaks": "^4.0.0",
    "remark-gfm": "^4.0.1",
    "remark-parse": "^11.0.0",
    "unified": "^11.0.5"
  },
  "devDependencies": {
    "@electron-forge/cli": "7.11.2",
    "@electron-forge/maker-zip": "7.11.2",
    "@electron-forge/plugin-vite": "7.11.2",
    "@electron/fuses": "2.1.3",
    "@eslint/js": "^10.0.1",
    "@types/leaflet": "^1.9.22",
    "@types/node": "^22.15.0",
    "@types/react": "^18.3.26",
    "@types/react-dom": "^18.3.7",
    "@vitejs/plugin-react": "^5.0.0",
    "electron": "43.2.0",
    "eslint": "^10.8.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.9.0",
    "tsx": "^4.23.1",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.65.0",
    "vite": "^7.0.0",
    "wrangler": "4.127.1"
  },
  "config": {
    "forge": "./config/electron/forge.config.ts"
  }
}
