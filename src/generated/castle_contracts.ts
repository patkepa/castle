// Generated by scripts/generate-contracts.mjs from Rust castle-contracts DTOs.
// Do not edit this file directly.
import schemaDocument from "./castle_contract_schema.json";

export const CASTLE_RPC_PROTOCOL_VERSION = 1 as const;
export const CASTLE_CONTENT_CONTRACT_VERSION = 2 as const;

export interface CalendarEvent {
  id: string;
  noteId: string;
  route: string;
  date: string;
  startTime: string;
  endTime?: string | null;
  endDate?: string | null;
  recurrence?: CalendarEventRecurrence | null;
  repeatUntil?: string | null;
  title: string;
  description: string;
  kind: CalendarEventKind;
  people: Array<CalendarEventPerson>;
  project?: ProjectReference | null;
}

export type CalendarEventKind = "work" | "social";

export interface CalendarEventPerson {
  noteId: string;
  name: string;
  route: string;
}

export type CalendarEventRecurrence = "weekly";

export interface CalendarResource {
  contractVersion: number;
  generatedAt: string;
  calendarEvents: Array<CalendarEvent>;
}

export interface CatalogNote {
  id: string;
  section: string;
  sectionLabel: string;
  relativePath: string;
  sourceFile: string;
  route: string;
  title: string;
  excerpt: string;
  preview?: string | null;
  tags: Array<string>;
  aliases: Array<string>;
  status: string;
  avatarUrl: string;
  createdAt?: string | null;
  modifiedAt: string;
  contentPath: string;
  wordCount: number;
  readingMinutes: number;
  pinned: boolean;
  sidebar?: PersonNoteSidebar | null;
}

export interface CompilationDelta {
  contractVersion: number;
  generatedAt: string;
  sections: Array<SectionSummary>;
  folders: Array<LibraryFolder>;
  notes: EntityDelta;
  tasks: EntityDelta2;
  projects: EntityDelta3;
  calendarEvents: EntityDelta4;
  shortcutCollections: Array<ShortcutCollection>;
  mutableResourcePaths: Array<string>;
}

export interface CreateFolderInput {
  sourceDirectory: string;
}

export interface CreateFolderResult {
  sourceDirectory: string;
  generatedAt: string;
}

export interface CreateSourceInput {
  noteId: string;
  sourceFile: string;
  markdown: string;
}

export interface CreateTaskInput {
  fields: TaskFields;
}

export interface DeleteFolderInput {
  sourceDirectory: string;
  recursive: boolean;
}

export interface DeleteFolderResult {
  sourceDirectory: string;
  entryCount: number;
  trashId?: string | null;
  generatedAt: string;
}

export interface DeleteSourceInput {
  noteId: string;
  sourceFile: string;
  expectedRevision: string;
}

export interface DeleteSourceResult {
  noteId: string;
  sourceFile: string;
  generatedAt: string;
  trashId: string;
  sourceGeneration?: number | null;
  publicationPending?: boolean | null;
}

export interface DeleteTaskInput {
  taskId: string;
}

export interface DeleteTaskResult {
  source: DeleteSourceResult;
  task: Task;
}

export interface EntityDelta {
  upserted: Array<CatalogNote>;
  removedIds: Array<string>;
  orderedIds?: Array<string> | null;
}

export interface EntityDelta2 {
  upserted: Array<Task>;
  removedIds: Array<string>;
  orderedIds?: Array<string> | null;
}

export interface EntityDelta3 {
  upserted: Array<Project>;
  removedIds: Array<string>;
  orderedIds?: Array<string> | null;
}

export interface EntityDelta4 {
  upserted: Array<CalendarEvent>;
  removedIds: Array<string>;
  orderedIds?: Array<string> | null;
}

export interface GeneratedResourceDescriptor {
  path: string;
  sha256: string;
  itemCount: number;
}

export interface GeneratedResourceManifest {
  contractVersion: number;
  generatedAt: string;
  resources: Record<string, GeneratedResourceDescriptor>;
}

export interface KnowledgeBase {
  contractVersion: number;
  generatedAt: string;
  sections: Array<SectionSummary>;
  folders: Array<LibraryFolder>;
  notes: Array<CatalogNote>;
  calendarEvents: Array<CalendarEvent>;
  tasks: Array<Task>;
  projects: Array<Project>;
  shortcutCollections: Array<ShortcutCollection>;
}

export interface LibraryFolder {
  sectionId: string;
  directory: Array<string>;
  entryCount: number;
  noteCount: number;
}

export interface MoveSourceInput {
  noteId: string;
  sourceFile: string;
  destinationSourceFile: string;
  expectedRevision: string;
}

export interface MoveSourceResult {
  noteId: string;
  previousSourceFile: string;
  sourceFile: string;
  route: string;
  revision: string;
  generatedAt: string;
  sourceGeneration?: number | null;
  publicationPending?: boolean | null;
}

export interface MutateTaskInput {
  taskId: string;
  command: TaskCommand;
}

export interface NoteSidebarFact {
  label: string;
  value: string;
  href?: string | null;
}

export interface NotesResource {
  contractVersion: number;
  generatedAt: string;
  notes: Array<CatalogNote>;
}

export interface PersonContact {
  kind: PersonContactKind;
  label: string;
  value: string;
  detail: string;
  href: string;
}

export type PersonContactKind = "phone" | "email" | "address" | "website" | "social" | "other";

export interface PersonFields {
  name: string;
  nickname: string;
  birthday: string;
  birthplace: string;
  nationality: string;
  status: PersonStatus;
  alignments: Array<string>;
  relation: PersonRelation;
  knownFrom: Array<string>;
  company: string;
  departments: Array<string>;
  location: string;
  avatar: string;
  tags: Array<string>;
  met: string;
  metThrough: string;
  body: string;
}

export interface PersonMutationResult {
  source: SaveSourceResult;
  note: CatalogNote;
}

export interface PersonNoteSidebar {
  kind: string;
  title: string;
  avatarUrl: string;
  facts: Array<NoteSidebarFact>;
  contacts: Array<PersonContact>;
}

export type PersonRelation = "positive" | "neutral" | "flirty" | "mixed" | "negative";

export type PersonStatus = "active" | "former";

export interface Project {
  id: string;
  noteId: string;
  route: string;
  title: string;
  description: string;
  status: ProjectStatus;
  startedAt: string;
  completedAt: string;
  modifiedAt: string;
  tags: Array<string>;
  people: Array<TaskPerson>;
  taskIds: Array<string>;
  eventIds: Array<string>;
}

export interface ProjectReference {
  id: string;
  title: string;
  route: string;
}

export interface ProjectsResource {
  contractVersion: number;
  generatedAt: string;
  projects: Array<Project>;
}

export type ProjectStatus = "idea" | "planned" | "active" | "paused" | "completed" | "archived";

export interface ProtocolHandshake {
  protocolVersion: number;
  contentContractVersion: number;
  serverVersion: string;
  capabilities: Array<string>;
}

export interface RestoreSourceInput {
  noteId: string;
  sourceFile: string;
  trashId: string;
}

export interface RestoreTaskInput {
  taskId: string;
  noteId: string;
  sourceFile: string;
  trashId: string;
}

export interface SaveSourceInput {
  noteId: string;
  sourceFile: string;
  markdown: string;
  expectedRevision: string;
}

export interface SaveSourceResult {
  noteId: string;
  sourceFile: string;
  revision: string;
  generatedAt: string;
  sourceGeneration?: number | null;
  publicationPending?: boolean | null;
}

export interface SectionSummary {
  id: string;
  label: string;
  icon: string;
  count: number;
}

export interface ServiceState {
  protocol: ProtocolHandshake;
  generatedAt: string;
  publicRoot: string;
}

export interface Shortcut {
  category: string;
  label: string;
  description: string;
  href: string;
}

export interface ShortcutCollection {
  id: string;
  label: string;
  sortOrder: number;
  shortcuts: Array<Shortcut>;
}

export interface SourceDocument {
  noteId: string;
  sourceFile: string;
  markdown: string;
  revision: string;
}

export interface Task {
  id: string;
  noteId: string;
  route: string;
  title: string;
  description: string;
  status: TaskStatus;
  targetDate: string;
  targetTime: string;
  estimateMinutes: number;
  createdAt: string;
  completedAt: string;
  sortOrder: number;
  modifiedAt: string;
  tags: Array<string>;
  people: Array<TaskPerson>;
  project?: ProjectReference | null;
  subtasks: Array<TaskSubtask>;
}

export type TaskCommand = { fields: TaskFields; kind: "update" } | { status: TaskStatus; kind: "changeStatus" } | { status: TaskStatus; sortOrder: number; kind: "move" } | { subtaskId: string; kind: "toggleSubtask" } | { title: string; kind: "addSubtask" } | { subtaskId: string; kind: "removeSubtask" };

export interface TaskFields {
  title: string;
  description: string;
  status: TaskStatus;
  targetDate: string;
  targetTime: string;
  estimateMinutes: number;
  projectId: string;
  peopleIds: Array<string>;
  tags: Array<string>;
}

export interface TaskMutationResult {
  source: SaveSourceResult;
  task: Task;
}

export interface TaskPerson {
  noteId: string;
  name: string;
  route: string;
  avatarUrl: string;
}

export interface TasksResource {
  contractVersion: number;
  generatedAt: string;
  tasks: Array<Task>;
}

export type TaskStatus = "todo" | "in_progress" | "blocked" | "done";

export interface TaskSubtask {
  id: string;
  title: string;
  completed: boolean;
}

export interface UpdatePersonInput {
  noteId: string;
  fields: PersonFields;
}
export interface CastleContractMap {
  CalendarEvent: CalendarEvent;
  CalendarEventKind: CalendarEventKind;
  CalendarEventPerson: CalendarEventPerson;
  CalendarEventRecurrence: CalendarEventRecurrence;
  CalendarResource: CalendarResource;
  CatalogNote: CatalogNote;
  CompilationDelta: CompilationDelta;
  CreateFolderInput: CreateFolderInput;
  CreateFolderResult: CreateFolderResult;
  CreateSourceInput: CreateSourceInput;
  CreateTaskInput: CreateTaskInput;
  DeleteFolderInput: DeleteFolderInput;
  DeleteFolderResult: DeleteFolderResult;
  DeleteSourceInput: DeleteSourceInput;
  DeleteSourceResult: DeleteSourceResult;
  DeleteTaskInput: DeleteTaskInput;
  DeleteTaskResult: DeleteTaskResult;
  EntityDelta: EntityDelta;
  EntityDelta2: EntityDelta2;
  EntityDelta3: EntityDelta3;
  EntityDelta4: EntityDelta4;
  GeneratedResourceDescriptor: GeneratedResourceDescriptor;
  GeneratedResourceManifest: GeneratedResourceManifest;
  KnowledgeBase: KnowledgeBase;
  LibraryFolder: LibraryFolder;
  MoveSourceInput: MoveSourceInput;
  MoveSourceResult: MoveSourceResult;
  MutateTaskInput: MutateTaskInput;
  NoteSidebarFact: NoteSidebarFact;
  NotesResource: NotesResource;
  PersonContact: PersonContact;
  PersonContactKind: PersonContactKind;
  PersonFields: PersonFields;
  PersonMutationResult: PersonMutationResult;
  PersonNoteSidebar: PersonNoteSidebar;
  PersonRelation: PersonRelation;
  PersonStatus: PersonStatus;
  Project: Project;
  ProjectReference: ProjectReference;
  ProjectsResource: ProjectsResource;
  ProjectStatus: ProjectStatus;
  ProtocolHandshake: ProtocolHandshake;
  RestoreSourceInput: RestoreSourceInput;
  RestoreTaskInput: RestoreTaskInput;
  SaveSourceInput: SaveSourceInput;
  SaveSourceResult: SaveSourceResult;
  SectionSummary: SectionSummary;
  ServiceState: ServiceState;
  Shortcut: Shortcut;
  ShortcutCollection: ShortcutCollection;
  SourceDocument: SourceDocument;
  Task: Task;
  TaskCommand: TaskCommand;
  TaskFields: TaskFields;
  TaskMutationResult: TaskMutationResult;
  TaskPerson: TaskPerson;
  TasksResource: TasksResource;
  TaskStatus: TaskStatus;
  TaskSubtask: TaskSubtask;
  UpdatePersonInput: UpdatePersonInput;
}

type JsonSchema = {
  $ref?: string;
  anyOf?: JsonSchema[];
  oneOf?: JsonSchema[];
  const?: unknown;
  enum?: unknown[];
  type?: string | string[];
  properties?: Record<string, JsonSchema>;
  required?: string[];
  items?: JsonSchema;
  additionalProperties?: boolean | JsonSchema;
};

const contractDefinitions = (schemaDocument as unknown as {
  $defs: Record<string, JsonSchema>;
}).$defs;

export function parseCastleContract<Name extends keyof CastleContractMap>(
  name: Name,
  value: unknown,
): CastleContractMap[Name] {
  validateSchema(contractDefinitions[name], value, String(name), new Set());
  return value as CastleContractMap[Name];
}

function validateSchema(
  schema: JsonSchema,
  value: unknown,
  valuePath: string,
  references: Set<string>,
): void {
  if (schema.$ref) {
    const name = schema.$ref.split("/").at(-1);
    if (!name || !contractDefinitions[name]) {
      throw new Error(`Castle contract contains an unresolved reference at ${valuePath}.`);
    }
    const referenceKey = `${name}:${valuePath}`;
    if (references.has(referenceKey)) return;
    const nextReferences = new Set(references).add(referenceKey);
    validateSchema(contractDefinitions[name], value, valuePath, nextReferences);
    return;
  }
  if (schema.const !== undefined && value !== schema.const) invalid(valuePath);
  if (schema.enum && !schema.enum.includes(value)) invalid(valuePath);
  if (schema.anyOf || schema.oneOf) {
    const options = schema.anyOf ?? schema.oneOf ?? [];
    if (!options.some((option) => isValid(option, value, valuePath, references))) {
      invalid(valuePath);
    }
    return;
  }
  const types = Array.isArray(schema.type)
    ? schema.type
    : schema.type
      ? [schema.type]
      : [];
  if (types.length > 0 && !types.some((type) => matchesType(type, value))) {
    invalid(valuePath);
  }
  if (Array.isArray(value) && schema.items) {
    value.forEach((item, index) =>
      validateSchema(schema.items!, item, `${valuePath}[${index}]`, references)
    );
    return;
  }
  if (isRecord(value) && (schema.properties || schema.required)) {
    for (const key of schema.required ?? []) {
      if (!(key in value)) invalid(`${valuePath}.${key}`);
    }
    for (const [key, item] of Object.entries(value)) {
      const propertySchema = schema.properties?.[key];
      if (propertySchema) {
        validateSchema(propertySchema, item, `${valuePath}.${key}`, references);
      } else if (schema.additionalProperties === false) {
        invalid(`${valuePath}.${key}`);
      } else if (isRecord(schema.additionalProperties)) {
        validateSchema(
          schema.additionalProperties,
          item,
          `${valuePath}.${key}`,
          references,
        );
      }
    }
  }
}

function isValid(
  schema: JsonSchema,
  value: unknown,
  valuePath: string,
  references: Set<string>,
) {
  try {
    validateSchema(schema, value, valuePath, references);
    return true;
  } catch {
    return false;
  }
}

function matchesType(type: string, value: unknown) {
  if (type === "null") return value === null;
  if (type === "array") return Array.isArray(value);
  if (type === "object") return isRecord(value);
  if (type === "integer") return Number.isSafeInteger(value);
  if (type === "number") return typeof value === "number" && Number.isFinite(value);
  return typeof value === type;
}

function isRecord(value: unknown): value is Record<string, unknown> {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}

function invalid(valuePath: string): never {
  throw new Error(`Castle received invalid contract data at ${valuePath}.`);
}
