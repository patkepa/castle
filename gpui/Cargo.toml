[package]
name = "castle-gpui"
description = "Native GPUI feasibility prototype for Castle"
edition = "2024"
rust-version = "1.90"
version = "0.1.0"
license = "MIT"
publish = false

[dependencies]
anyhow = "1.0.103"
castle-core = { path = "../native/castle_core" }
gpui = "=0.2.2"

# Keep this experiment independent from native/Cargo.toml. In particular, normal
# Castle workspace checks should not need to compile GPUI and its platform stack.
[workspace]
